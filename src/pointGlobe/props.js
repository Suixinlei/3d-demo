const props = {
  initCameraDistance: 300,
  globeRadius: 256,
  innerGlobeRadius: 250,
  mapSize: {
    // Size of the map from the intial source image (on which the dots are positioned on)
    width: 666 / 2,
    height: 322 / 2
  },
};

module.exports = props;
